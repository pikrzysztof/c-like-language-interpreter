The Language gramatyka
BNF Converter


%This txt2tags file is machine-generated by the BNF-converter
%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of gramatyka==
===Identifiers===
Identifiers //Ident// are unquoted strings beginning with a letter,
followed by any combination of letters, digits, and the characters ``_ '``
reserved words excluded.


===Literals===
Integer literals //Integer// are nonempty sequences of digits.


Double-precision float literals //Double// have the structure
indicated by the regular expression ``digit+ '.' digit+ ('e' ('-')? digit+)?`` i.e.\
two sequences of digits separated by a decimal point, optionally
followed by an unsigned or negative exponent.


String literals //String// have the form
``"``//x//``"``}, where //x// is any sequence of any characters
except ``"`` unless preceded by ``\``.




===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in gramatyka are the following:
  | ``and`` | ``boolean`` | ``complex`` | ``cond``
  | ``const`` | ``dump`` | ``else`` | ``exit``
  | ``extern`` | ``false`` | ``for`` | ``if``
  | ``input`` | ``int`` | ``list`` | ``load``
  | ``matrix`` | ``measure`` | ``mod`` | ``not``
  | ``operator`` | ``or`` | ``print`` | ``procedure``
  | ``qucond`` | ``quconst`` | ``qufunct`` | ``qureg``
  | ``quscratch`` | ``quvoid`` | ``real`` | ``reset``
  | ``return`` | ``save`` | ``set`` | ``shell``
  | ``step`` | ``string`` | ``tensor`` | ``to``
  | ``true`` | ``until`` | ``vector`` | ``while``
  | ``xor`` |  |  | 

The symbols used in gramatyka are the following:
  | , | ( | ) | [
  | ] | : | :: | ..
  | \ | == | != | <
  | <= | > | >= | +
  | - | & | * | /
  | ^ | # | ! | ;
  | = | -> | <- | <->
  | { | } |  | 

===Comments===
Single-line comments begin with //.Multiple-line comments are  enclosed with /* and */.

==The syntactic structure of gramatyka==
Non-terminals are enclosed between < and >. 
The symbols -> (production),  **|**  (union) 
and **eps** (empty rule) belong to the BNF notation. 
All other symbols are terminals.

  | //Program// | -> | //[Def]// //[Stmt]// 
  | //[Ident]// | -> | **eps**
  |  |  **|**  | //Ident// 
  |  |  **|**  | //Ident// ``,`` //[Ident]// 
  | //Const// | -> | //Integer// 
  |  |  **|**  | ``(`` //Double// ``,`` //Double// ``)`` 
  |  |  **|**  | ``true`` 
  |  |  **|**  | ``false`` 
  |  |  **|**  | //String// 
  | //[Expr]// | -> | **eps**
  |  |  **|**  | //Expr// 
  |  |  **|**  | //Expr// ``,`` //[Expr]// 
  | //Expr11// | -> | //Ident// 
  |  |  **|**  | //Ident// ``(`` //[Expr]// ``)`` 
  |  |  **|**  | //Ident// ``[`` //Expr// ``]`` 
  |  |  **|**  | //Ident// ``[`` //Expr// ``,`` //Expr// ``]`` 
  |  |  **|**  | //Ident// ``[`` //Expr// ``:`` //Expr// ``]`` 
  |  |  **|**  | //Ident// ``[`` //Expr// ``::`` //Expr// ``]`` 
  |  |  **|**  | //Ident// ``[`` //Expr// ``..`` //Expr// ``]`` 
  |  |  **|**  | //Ident// ``[`` //Expr// ``\`` //Expr// ``]`` 
  |  |  **|**  | //Const// 
  |  |  **|**  | ``(`` //Expr// ``)`` 
  | //Expr// | -> | //Expr// ``==`` //Expr1// 
  |  |  **|**  | //Expr// ``!=`` //Expr1// 
  |  |  **|**  | //Expr1// 
  | //Expr1// | -> | //Expr1// ``<`` //Expr2// 
  |  |  **|**  | //Expr1// ``<=`` //Expr2// 
  |  |  **|**  | //Expr1// ``>`` //Expr2// 
  |  |  **|**  | //Expr1// ``>=`` //Expr2// 
  |  |  **|**  | //Expr2// 
  | //Expr2// | -> | //Expr2// ``or`` //Expr3// 
  |  |  **|**  | //Expr3// 
  | //Expr3// | -> | //Expr3// ``and`` //Expr4// 
  |  |  **|**  | //Expr4// 
  | //Expr4// | -> | //Expr4// ``xor`` //Expr5// 
  |  |  **|**  | //Expr5// 
  | //Expr5// | -> | ``not`` //Expr6// 
  |  |  **|**  | //Expr6// 
  | //Expr6// | -> | //Expr6// ``+`` //Expr7// 
  |  |  **|**  | //Expr6// ``-`` //Expr7// 
  |  |  **|**  | //Expr6// ``&`` //Expr7// 
  |  |  **|**  | //Expr7// 
  | //Expr7// | -> | //Expr7// ``*`` //Expr8// 
  |  |  **|**  | //Expr7// ``/`` //Expr8// 
  |  |  **|**  | //Expr7// ``mod`` //Expr8// 
  |  |  **|**  | //Expr8// 
  | //Expr8// | -> | //Expr8// ``^`` //Expr9// 
  |  |  **|**  | //Expr9// 
  | //Expr9// | -> | ``-`` //Expr10// 
  |  |  **|**  | //Expr10// 
  | //Expr10// | -> | ``#`` //Expr11// 
  |  |  **|**  | //Expr11// 
  | //Option// | -> | //Ident// 
  | //[Stmt]// | -> | **eps**
  |  |  **|**  | //Stmt// //[Stmt]// 
  | //Stmt// | -> | ``!`` //Ident// ``(`` //[Expr]// ``)`` ``;`` 
  |  |  **|**  | //Expr// ``;`` 
  |  |  **|**  | //Expr// ``=`` //Expr// ``;`` 
  |  |  **|**  | //Expr// //FanoutSugarOp// //Expr// ``;`` 
  |  |  **|**  | ``for`` //Ident// ``=`` //Expr// ``to`` //Expr// //Block// 
  |  |  **|**  | ``for`` //Ident// ``=`` //Expr// ``to`` //Expr// ``step`` //Expr// //Block// 
  |  |  **|**  | ``while`` //Expr// //Block// 
  |  |  **|**  | //Block// ``until`` //Expr// ``;`` 
  |  |  **|**  | ``if`` //Expr// //Block// 
  |  |  **|**  | ``if`` //Expr// //Block// ``else`` //Block// 
  |  |  **|**  | ``return`` //Expr// ``;`` 
  |  |  **|**  | ``input`` //Expr// //Ident// ``;`` 
  |  |  **|**  | ``input`` //Ident// ``;`` 
  |  |  **|**  | ``print`` //[Expr]// ``;`` 
  |  |  **|**  | ``exit`` //Expr// ``;`` 
  |  |  **|**  | ``measure`` //Expr// ``;`` 
  |  |  **|**  | ``measure`` //Expr// ``,`` //Ident// ``;`` 
  |  |  **|**  | ``reset`` ``;`` 
  |  |  **|**  | ``list`` //[Ident]// ``;`` 
  |  |  **|**  | ``dump`` //Expr// ``;`` 
  |  |  **|**  | ``dump`` ``;`` 
  |  |  **|**  | ``load`` //Expr// ``;`` 
  |  |  **|**  | ``load`` ``;`` 
  |  |  **|**  | ``save`` //Expr// ``;`` 
  |  |  **|**  | ``save`` ``;`` 
  |  |  **|**  | ``shell`` ``;`` 
  |  |  **|**  | ``set`` //Option// //Expr// ``;`` 
  |  |  **|**  | ``;`` 
  | //FanoutSugarOp// | -> | ``->`` 
  |  |  **|**  | ``<-`` 
  |  |  **|**  | ``<->`` 
  | //Type// | -> | //ST// 
  |  |  **|**  | //ST// ``vector`` 
  |  |  **|**  | //ST// ``matrix`` 
  |  |  **|**  | //ST// ``tensor`` //Integer// 
  | //ST// | -> | ``string`` 
  |  |  **|**  | ``boolean`` 
  |  |  **|**  | ``int`` 
  |  |  **|**  | ``real`` 
  |  |  **|**  | ``complex`` 
  |  |  **|**  | ``qureg`` 
  |  |  **|**  | ``quvoid`` 
  |  |  **|**  | ``quconst`` 
  |  |  **|**  | ``quscratch`` 
  |  |  **|**  | ``qucond`` 
  | //ConstDef// | -> | ``const`` //Ident// ``=`` //Expr// 
  |  |  **|**  | //Type// //Ident// ``=`` ``cond`` 
  | //VarDef// | -> | //Type// //Ident// 
  |  |  **|**  | //Type// //Ident// ``=`` //Expr// 
  |  |  **|**  | //Type// //Ident// ``[`` //Expr// ``]`` 
  | //[ArgDef]// | -> | **eps**
  |  |  **|**  | //ArgDef// 
  |  |  **|**  | //ArgDef// ``,`` //[ArgDef]// 
  | //ArgDef// | -> | //Type// //Ident// 
  | //[ConstOrVar]// | -> | **eps**
  |  |  **|**  | //ConstOrVar// ``;`` //[ConstOrVar]// 
  | //ConstOrVar// | -> | //ConstDef// 
  |  |  **|**  | //VarDef// 
  | //Body// | -> | ``{`` //[ConstOrVar]// //[Stmt]// ``}`` 
  | //[Def]// | -> | **eps**
  |  |  **|**  | //Def// //[Def]// 
  | //Def// | -> | //ConstDef// ``;`` 
  |  |  **|**  | //VarDef// ``;`` 
  |  |  **|**  | //Type// //Ident// ``(`` //[ArgDef]// ``)`` //Body// 
  |  |  **|**  | ``procedure`` //Ident// ``(`` //[ArgDef]// ``)`` //Body// 
  |  |  **|**  | ``operator`` //Ident// ``(`` //[ArgDef]// ``)`` //Body// 
  |  |  **|**  | ``qufunct`` ``operator`` //Ident// ``(`` //[ArgDef]// ``)`` //Body// 
  |  |  **|**  | ``cond`` ``operator`` //Ident// ``(`` //[ArgDef]// ``)`` //Body// 
  |  |  **|**  | ``qufunct`` //Ident// ``(`` //[ArgDef]// ``)`` //Body// 
  |  |  **|**  | ``cond`` ``qufunct`` //Ident// ``(`` //[ArgDef]// ``)`` //Body// 
  |  |  **|**  | ``extern`` ``operator`` //Ident// ``(`` //[ArgDef]// ``)`` ``;`` 
  |  |  **|**  | ``extern`` ``qufunct`` //Ident// ``(`` //[ArgDef]// ``)`` ``;`` 
  |  |  **|**  | ``extern`` ``cond`` ``operator`` //Ident// ``(`` //[ArgDef]// ``)`` ``;`` 
  |  |  **|**  | ``extern`` ``cond`` ``qufunct`` //Ident// ``(`` //[ArgDef]// ``)`` ``;`` 
  | //Block// | -> | ``{`` //[Stmt]// ``}`` 


